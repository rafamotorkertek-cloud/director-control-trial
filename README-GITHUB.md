# Director Control — GitHub Mobile Trial

Upload these files to the GitHub repository root:
- server.js
- package.json
- render.yaml
- .gitignore
- RENDER-TRIAL.md

The web interface is embedded inside server.js, so no folder upload is needed.
Then connect the repository to Render as a Web Service.
